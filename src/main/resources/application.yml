server:
  port: 8083
spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/sentinel?currentSchema=notification
    username: sentinel
    password: sentinel
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: notification
  data:
    redis:
      host: localhost
      port: 6379
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
